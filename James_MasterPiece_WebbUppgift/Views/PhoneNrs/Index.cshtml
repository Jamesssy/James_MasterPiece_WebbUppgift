@model IEnumerable<JMP_WU_Domain.PhoneNr>

@{
    ViewData["Title"] = "Index";
}

<h2>Phonenumbers</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
@using (Html.BeginForm())
{
    <p>
        Search: @Html.TextBox("SearchString")
        <input type="submit" value="Search" />
    </p>
}
<table class="table">
    <thead>
        <tr>
                <th>
                    @Html.ActionLink("Main Phonenumber", "Index", new { sortOrder = ViewBag.FirstNameSortParm })
                </th>
                <th>
                    @Html.ActionLink("Alternative Phonenumber 1", "Index", new { sortOrder = ViewBag.Alt1PhoneNrSortParm })
                </th>
                <th>
                    @Html.ActionLink("Alternative Phonenumber 2", "Index", new { sortOrder = ViewBag.Alt2PhoneNrSortParm })
                </th>
            <th>
                @Html.ActionLink("Employee", "Index", new { sortOrder = ViewBag.FirstNameSortParm })
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MainPhoneNr)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AltPhoneNr1)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.AltPhoneNr2)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Employee.FirstName) @Html.DisplayFor(modelItem => item.Employee.LastName)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
